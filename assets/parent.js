(()=>{'use strict';const P=window.AcademyPro,$=id=>document.getElementById(id),DAY=86400000,esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function masteryFlat(o){const a=[];(function w(x){Object.values(x||{}).forEach(v=>v&&typeof v==='object'&&'lessonId'in v?a.push(v):w(v))})(o);return a}function quizTrend(profile){const rows=Object.values(profile?.quizHistory||{}).filter(x=>x?.createdAt),now=Date.now(),week=rows.filter(x=>x.createdAt>=now-7*DAY),prev=rows.filter(x=>x.createdAt<now-7*DAY&&x.createdAt>=now-14*DAY),avg=a=>a.length?Math.round(a.reduce((n,x)=>n+Number(x.score||0),0)/a.length):0;return{current:avg(week),previous:avg(prev)}}function alertsFor(r,avg,due){const a=[],last=Number(r.profile?.lastActiveAt||0),days=last?Math.floor((Date.now()-last)/DAY):null,trend=quizTrend(r.profile);if(days===null||days>=5)a.push({tone:'bad',icon:'⚠️',text:days===null?'لا يوجد نشاط مسجل للطالب حتى الآن.':'لم يدخل المنصة منذ '+days+' أيام.'});if(due>=5)a.push({tone:'warn',icon:'🧠',text:'لديه '+due+' أسئلة مستحقة في المراجعة الذكية.'});if(avg>0&&avg<60)a.push({tone:'bad',icon:'📉',text:'متوسط الإتقان الحالي '+avg+'% ويحتاج دعمًا ومراجعة.'});if(trend.current&&trend.previous&&trend.current-trend.previous>=10)a.push({tone:'ok',icon:'📈',text:'تحسن متوسط الاختبارات '+(trend.current-trend.previous)+' نقاط عن الأسبوع السابق.'});if(!a.length)a.push({tone:'ok',icon:'✅',text:'لا توجد تنبيهات مهمة حاليًا، استمر في المتابعة.'});return a}function render(id,r){const m=masteryFlat(r.mastery),avg=m.length?Math.round(m.reduce((s,x)=>s+Number(x.score||0),0)/m.length):0,due=Object.values(r.reviews||{}).filter(x=>x.status!=='mastered'&&x.nextReviewAt<=Date.now()).length,attendance=Object.values(r.analytics?.attendance||{}),minutes=Math.round(attendance.reduce((n,x)=>n+Number(x.totalSeconds||0),0)/60),alerts=alertsFor(r,avg,due),last=r.profile?.lastActiveAt?new Date(r.profile.lastActiveAt).toLocaleString('ar-EG'):'لا يوجد نشاط';return '<article class="pro-card wide"><div class="pro-space"><div><span class="pro-badge">طالب مرتبط</span><h2>'+esc(r.profile?.name||'طالب')+'</h2><div class="pro-muted">'+esc(r.profile?.stage||'')+' • الصف '+esc(r.profile?.grade||'')+' • آخر نشاط: '+esc(last)+'</div></div><div><div class="pro-kpi">'+avg+'%</div><div class="pro-muted">متوسط الإتقان</div></div></div><div class="pro-list"><div class="pro-item"><div class="pro-space"><strong>XP والمستوى</strong><span>'+(r.xp.total||0)+' XP • مستوى '+(r.xp.level||1)+'</span></div></div><div class="pro-item"><div class="pro-space"><strong>مراجعات مستحقة</strong><span class="pro-badge '+(due?'bad':'ok')+'">'+due+'</span></div></div><div class="pro-item"><div class="pro-space"><strong>الدروس المتقنة</strong><span>'+m.filter(x=>x.status==='mastered').length+' من '+m.length+'</span></div></div><div class="pro-item"><div class="pro-space"><strong>حضور الحصص المباشرة</strong><span>'+minutes+' دقيقة</span></div></div></div><h3 style="margin-top:18px">تنبيهات مهمة</h3><div class="pro-list">'+alerts.map(x=>'<div class="pro-item"><span class="pro-badge '+x.tone+'">'+x.icon+' '+esc(x.text)+'</span></div>').join('')+'</div></article>'}async function load(){P.auth.onAuthStateChanged(async u=>{if(!u){location.href='./index.html';return}const ids=await P.getChildren(u.uid),box=$('children');if(!ids.length){box.innerHTML='<article class="pro-card full pro-empty">لا يوجد طلاب مربوطون بهذا الحساب بعد.</article>';return}box.innerHTML='';for(const id of ids){try{box.insertAdjacentHTML('beforeend',render(id,await P.parentReport(id)))}catch(e){console.error(e);box.insertAdjacentHTML('beforeend','<article class="pro-card full pro-empty">تعذر تحميل تقرير طالب مرتبط.</article>')}}})}$('linkBtn').onclick=async()=>{try{await P.linkParent($('parentInvite').value.trim());await P.db.ref('parentProfilesV4/'+P.auth.currentUser.uid).update({updatedAt:Date.now()});location.reload()}catch(e){alert(e.message||'تعذر الربط')}};load()})();